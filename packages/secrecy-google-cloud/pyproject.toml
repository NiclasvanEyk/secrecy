[project]
name = "secrecy-google-cloud"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "google-cloud-secret-manager>=2.20.2",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
