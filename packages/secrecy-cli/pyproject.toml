[project]
name = "secrecy-cli"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = ["click>=8.1.7", "pydantic>=2.9.2", "rich>=13.9.2", "secrecy"]

[project.scripts]
secrecy = "secrecy_cli.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.secrecy.secrets]
modules = ["secrecy_cli.demo_secrets"]
