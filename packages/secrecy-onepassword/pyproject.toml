[project]
name = "secrecy-onepassword"
dynamic = ["version"]
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = ["onepassword-sdk>=0.1.2", "secrecy"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.version]
path = "src/secrecy_onepassword/__about__.py"
